# Backend API base URL for production builds.
# Leave blank in local dev (Vite proxies /api → http://localhost:8000).
# In production set it to your deployed backend, e.g.:
#   VITE_API_BASE=https://navora-api.onrender.com
VITE_API_BASE=
